import type {MetadataRoute} from "next";import {db} from "@/lib/db";import {env} from "@/lib/env";
export default async function sitemap():Promise<MetadataRoute.Sitemap>{const base=env.NEXT_PUBLIC_SITE_URL;try{const [qs,cats]=await Promise.all([db.question.findMany({where:{status:"PUBLISHED"},select:{slug:true,updatedAt:true}}),db.category.findMany({where:{active:true},select:{slug:true,updatedAt:true}})]);return[{url:base,lastModified:new Date()},...cats.map(c=>({url:`${base}/category/${c.slug}`,lastModified:c.updatedAt})),...qs.map(q=>({url:`${base}/q/${q.slug}`,lastModified:q.updatedAt}))]}catch{return[{url:base,lastModified:new Date()}]}}
