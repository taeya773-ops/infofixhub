import type { Metadata } from "next";
import Link from "next/link";import {notFound} from "next/navigation";import {db} from "@/lib/db";
export const dynamic="force-dynamic";
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;try{const category=await db.category.findUnique({where:{slug},select:{name:true,description:true}});if(!category)return{};return{title:category.name,description:category.description,alternates:{canonical:`/category/${encodeURIComponent(slug)}`}}}catch{return{alternates:{canonical:`/category/${encodeURIComponent(slug)}`}}}}
export default async function CategoryPage({params,searchParams}:{params:Promise<{slug:string}>;searchParams:Promise<{page?:string}>}){const {slug}=await params;const page=Math.max(1,Number((await searchParams).page)||1);let category;try{category=await db.category.findUnique({where:{slug},include:{questions:{where:{status:"PUBLISHED"},orderBy:{publishedAt:"desc"},skip:(page-1)*12,take:12}}})}catch{notFound()}if(!category)notFound();return <main className="wrap section"><div className="eyebrow">Category</div><h1>{category.name}</h1><p className="lead">{category.description}</p><div className="grid">{category.questions.map(q=><Link className="card" href={`/q/${q.slug}`} key={q.id}><h3>{q.title}</h3></Link>)}</div></main>}
